apiVersion: apps/v1
kind: Deployment
metadata:
  name: learnreact-frontend
  namespace: learnreact
  labels:
    app: learnreact-frontend
    tier: frontend
spec:
  replicas: 2  # Number of frontend pod replicas
  selector:
    matchLabels:
      app: learnreact-frontend
  template:
    metadata:
      labels:
        app: learnreact-frontend
        tier: frontend
    spec:
      containers:
        - name: frontend
          image: your-gitlab-registry/learnreact/frontend:latest  # Replace with your registry
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: learnreact-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
      volumes:
        - name: nginx-config
          configMap:
            name: nginx-config
      restartPolicy: Always
